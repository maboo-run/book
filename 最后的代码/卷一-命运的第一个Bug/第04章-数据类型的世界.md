"今天我们要深入学习Python的数据类型。"

马老师打开投影，屏幕上显示着四个大字：int、float、str、bool。

"昨天我们学了变量，知道变量就像盒子，可以装数据。但是，这些盒子能装什么？装进去的东西有什么区别？这就是今天要讲的——数据类型。"

林北坐在座位上，笔记本已经打开。他昨晚又遇到了好几次`TypeError`，深深感受到了类型的重要性。

"Python有四种最基本的数据类型，"马老师在白板上写下：

```python
# 整数 (int) - 用来数数和计算
age = 22

# 浮点数 (float) - 用来表示小数
height = 1.75

# 字符串 (str) - 用来存储文字
name = "林北"

# 布尔值 (bool) - 用来表示真假
is_student = True
```

"这四种类型，就像四种不同的容器。装水的瓶子不能装沙子，装沙子的袋子不能装水。"

"老师，"李明举手，"整数和浮点数有什么区别？不都是数字吗？"

"好问题。"马老师笑了，"让我给你们做个实验。"

他在屏幕上输入：

```python
a = 10
b = 10.0

print(a)
print(b)
print(type(a))
print(type(b))
```

运行，输出：

```
10
10.0
<class 'int'>
<class 'float'>
```

"看到了吗？虽然数值相同，但类型不同。"

"整数是精确的，没有小数点。浮点数是带小数点的，可以表示更精细的数值。"

"比如，你的年龄是22岁，这是整数。但你的身高是1.75米，这是浮点数。"

王胖子嘀咕道："那我的体重……"

"也是浮点数。"马老师接话，"除非你只报整数，比如'75公斤'。"

林北在笔记本上记下：整数用于计数，浮点数用于测量。

"现在我们来讲讲类型转换。"马老师说，"有时候，你需要把一种类型转换成另一种类型。"

"比如，用户输入的永远是字符串。如果你想拿它来计算，就得先转成数字。"

```python
# 字符串转整数
age_str = "22"
age_int = int(age_str)
print(age_int + 1)  # 输出：23

# 字符串转浮点数
height_str = "1.75"
height_float = float(height_str)
print(height_float + 0.05)  # 输出：1.8

# 数字转字符串
age = 22
age_str = str(age)
print("我今年" + age_str + "岁")  # 输出：我今年22岁
```

"记住这三个函数：`int()`、`float()`、`str()`。它们就像三个变形金刚，可以把数据变来变去。"

林北立刻想到了自己昨天遇到的问题。原来那个`TypeError`，就是因为没有用`str()`把数字转成字符串！

"大家自己试试，"马老师说，"写一个程序，接收用户输入的身高和体重，然后计算BMI指数。公式是：BMI = 体重 / (身高 × 身高)。"

林北开始动手：

```python
height = input("请输入身高（米）：")
weight = input("请输入体重（公斤）：")

bmi = weight / (height * height)
print("你的BMI指数是：" + bmi)
```

保存，运行。

输入身高：1.75
输入体重：70

然后……报错了：

```
TypeError: unsupported operand type(s) for /: 'str' and 'str'
```

林北愣住了。又是`TypeError`？

他仔细看了看代码。哦，对了！`input()`返回的是字符串，不能直接用来计算！

他修改代码：

```python
height = float(input("请输入身高（米）："))
weight = float(input("请输入体重（公斤）："))

bmi = weight / (height * height)
print("你的BMI指数是：" + str(bmi))
```

再次运行。

输入身高：1.75
输入体重：70

```
你的BMI指数是：22.857142857142858
```

成功了！虽然小数点后面有一大堆数字，但至少能跑了。

就在这时，王胖子那边传来了声音："老师！我的程序又炸了！"

众人看过去，只见王胖子的屏幕上显示着：

```
ValueError: invalid literal for int() with base 10: '一百'
```

"你输入了什么？"马老师走过去。

"我输入'一百'啊。"王胖子理直气壮，"一百不就是100吗？"

马老师哭笑不得："Python不认识中文数字。`int()`只能转换阿拉伯数字的字符串，比如'100'。"

"那中文数字怎么办？"王胖子不服。

"得写个转换函数，把'一百'翻译成'100'。"马老师说，"这个太复杂了，现在不讲。你老老实实用阿拉伯数字吧。"

王胖子嘟囔着改了输入，这次终于成功了。

林北在心里记下：类型转换只能转换"看起来像"的数据。'100'可以转成数字，'一百'不行。

"大家注意，"马老师提醒道，"类型转换是有风险的。"

他在屏幕上演示：

```python
# 这个会成功
num = int("123")
print(num)  # 输出：123

# 这个会失败
num = int("123.45")
print(num)  # 报错！
```

运行，果然报错：

```
ValueError: invalid literal for int() with base 10: '123.45'
```

"为什么？"有同学问。

"`int()`不认识小数点。"马老师解释道，"如果你想把'123.45'转成整数，得先转成浮点数，再转成整数。"

```python
num = int(float("123.45"))
print(num)  # 输出：123
```

"注意，这里会丢掉小数部分。123.45变成了123，不是124。这叫截断，不是四舍五入。"

林北若有所思。原来类型转换还有这么多门道。

"现在我们来讲讲布尔值。"马老师说，"布尔值只有两个：`True`和`False`。"

"它是用来判断真假的。比如，'林北是学生'是真的，'林北会飞'是假的。"

```python
is_student = True
can_fly = False

print(is_student)  # 输出：True
print(can_fly)     # 输出：False
```

"布尔值在条件判断中非常重要。以后我们学`if`语句的时候，就会大量用到。"

"另外，Python中，很多东西都可以转换成布尔值。"

马老师演示：

```python
# 数字：0是False，其他都是True
print(bool(0))     # False
print(bool(1))     # True
print(bool(-5))    # True

# 字符串：空字符串是False，其他都是True
print(bool(""))    # False
print(bool("林北")) # True

# None：表示"什么都没有"，是False
print(bool(None))  # False
```

"这个叫'真值测试'。记住一个原则：空的、零的、None，都是假；其他都是真。"

林北在笔记本上写下：0、空字符串、None = False；其他 = True。

"老师，我有个问题。"苏晴举手。

所有人都看向她。这是她今天第一次发言。

"如果我想判断用户输入的是不是数字，应该怎么做？"

"好问题！"马老师眼睛一亮，"这是个实际应用的场景。"

"有几种方法。第一种，用`try-except`捕获异常。但这个我们还没学。"

"第二种，用字符串的方法：`isdigit()`。"

```python
user_input = input("请输入一个数字：")

if user_input.isdigit():
    num = int(user_input)
    print("你输入的数字是：", num)
else:
    print("这不是数字！")
```

"但是，`isdigit()`只能判断正整数。如果用户输入'-5'或'3.14'，它也会说不是数字。"

"所以，最好的方法还是用`try-except`。不过这个我们下节课再讲。"

苏晴点点头，若有所思。

下课前，马老师布置了作业：

"写一个程序，接收用户输入的出生年份，计算用户的年龄，并判断是否成年（>=18岁）。"

"提示：当前年份是2024。"

林北立刻开始动手：

```python
birth_year = int(input("请输入你的出生年份："))
current_year = 2024

age = current_year - birth_year
is_adult = age >= 18

print("你的年龄是：" + str(age))
print("是否成年：" + str(is_adult))
```

运行，输入2002。

```
你的年龄是：22
是否成年：True
```

完美！

但林北突然想到一个问题：如果用户输入的不是数字怎么办？如果用户输入2050怎么办？

看来，还需要加入数据验证。但这个……好像涉及到条件判断了。

下课后，林北、王胖子和苏晴一起走出教室。

"今天讲的类型转换，感觉还挺重要的。"林北说。

"对啊，"王胖子附和道，"我都不知道被`TypeError`折磨了多少次了。"

"其实只要记住一个原则，"苏晴轻声说，"用户输入的是字符串，计算需要数字，打印需要字符串。"

"所以流程是：字符串 → 数字 → 计算 → 数字 → 字符串 → 打印。"

林北眼睛一亮："对啊！就像一个流水线！"

"嗯。"苏晴微笑，"类型转换就是这个流水线上的关键环节。"

三人走到地铁站，分别的时候，王胖子突然说："你们说，我们真的能学会编程吗？"

"能。"苏晴很肯定，"只要理解了基础概念，后面就容易了。"

"我觉得也是，"林北说，"虽然现在还有很多不懂的，但至少每天都在进步。"

"那就好。"王胖子松了口气，"我就怕自己太笨，跟不上。"

"不会的。"林北拍拍他的肩膀，"你看李明，懂那么多理论，不也经常搞混？关键是多练。"

回到出租屋，林北开始复习今天学的内容。

他新建了一个文件`types_practice.py`，开始尝试各种类型转换：

```python
# 整数和浮点数的转换
a = 10
b = float(a)
print(b)  # 10.0

c = 3.14
d = int(c)
print(d)  # 3（截断，不是四舍五入）

# 字符串和数字的转换
age_str = "22"
age_int = int(age_str)
print(age_int + 1)  # 23

# 布尔值的真值测试
print(bool(0))      # False
print(bool(""))     # False
print(bool(None))   # False
print(bool(1))      # True
print(bool("林北")) # True
```

运行一遍，全部正确。

林北又想到了一个问题：如果把布尔值转成整数会怎样？

他试了试：

```python
print(int(True))   # 输出：1
print(int(False))  # 输出：0
```

"原来`True`等于1，`False`等于0！"林北恍然大悟。

这也解释了为什么`bool(1)`是`True`，`bool(0)`是`False`。

他又试了把字符串转成布尔值：

```python
print(bool("True"))   # 输出：True
print(bool("False"))  # 输出：True
```

"咦？"林北愣住了，"为什么'False'也是`True`？"

他想了想，明白了："因为'False'是一个非空字符串，所以它的布尔值是`True`！"

"只有空字符串''才是`False`。"

林北感叹，类型转换真是充满了细节和陷阱。

就在这时，他突然想到了马老师留的作业——判断是否成年。

他的代码中用了`age >= 18`，这个表达式返回的就是布尔值！

```python
age = 22
is_adult = age >= 18  # 这里返回True
```

原来，比较运算符（`>=`、`>`、`<`、`<=`、`==`、`!=`）的结果都是布尔值！

林北兴奋地写下：

```python
# 比较运算返回布尔值
print(10 > 5)    # True
print(10 < 5)    # False
print(10 == 10)  # True
print(10 != 5)   # True
```

他越写越兴奋，感觉自己对数据类型的理解又深入了一层。

但他也意识到，还有很多问题没有解决：

- 如果用户输入的不是数字怎么办？
- 如何判断用户的输入是否合法？
- 如何根据不同的条件执行不同的代码？

这些问题，都需要用到"条件判断"。

林北想起马老师说过，下节课要讲`if`语句。

第二天，林北早早来到教室。

他发现黑板上写着一行字："今天学习：数据类型的深入应用——类型陷阱与避坑指南"。

马老师走进教室，开门见山地说："昨天我们学了数据类型和类型转换。今天我们来看看，实际编程中会遇到哪些坑。"

他在屏幕上打出第一个例子：

```python
# 陷阱1：字符串拼接 vs 数字相加
a = "10"
b = "20"
print(a + b)  # 输出：1020（字符串拼接）

c = 10
d = 20
print(c + d)  # 输出：30（数字相加）
```

"看到了吗？同样是加号，对字符串是拼接，对数字是相加。"

"这就是为什么类型很重要。如果你不清楚变量的类型，结果可能完全出乎意料。"

林北想起昨天自己犯的错，深有同感。

"陷阱2：浮点数的精度问题。"马老师继续说。

```python
a = 0.1
b = 0.2
c = a + b
print(c)  # 输出：0.30000000000000004
```

"什么？"王胖子惊呼，"0.1加0.2不等于0.3？"

"在计算机里，不等于。"马老师严肃地说，"这是浮点数的精度问题。"

"计算机用二进制存储浮点数，而0.1在二进制里是个无限循环小数。就像1/3在十进制里是0.333...一样。"

"所以，永远不要用`==`比较两个浮点数是否相等。"

```python
# 错误的做法
if 0.1 + 0.2 == 0.3:
    print("相等")
else:
    print("不相等")  # 会输出这个

# 正确的做法
if abs((0.1 + 0.2) - 0.3) < 0.00001:
    print("足够接近")
```

"判断两个浮点数是否相等，应该看它们的差值是否足够小。"

林北认真记下。这是个重要的坑。

"陷阱3：整数除法。"马老师说。

```python
# Python 3中，/是真除法，//是整除
print(10 / 3)   # 输出：3.3333333333333335
print(10 // 3)  # 输出：3
```

"注意，`//`是整除，会丢弃小数部分。如果你想要小数结果，用`/`；如果你只想要整数部分，用`//`。"

"另外，还有一个取余运算符`%`，返回除法的余数。"

```python
print(10 % 3)  # 输出：1（10除以3余1）
```

李明举手："老师，为什么要有整除和取余？"

"因为有时候你需要。"马老师举例，"比如，你有100个苹果，3个人分，每人分几个？剩几个？"

```python
apples = 100
people = 3

per_person = apples // people  # 每人33个
remainder = apples % people     # 剩1个

print(f"每人分{per_person}个，剩{remainder}个")
```

"这就是整除和取余的实际应用。"

"陷阱4：类型转换的异常。"马老师说。

```python
# 这个会报错
age = int("二十二")  # ValueError
```

"如果转换失败，程序会崩溃。所以，在转换之前，最好先验证数据。"

"验证的方法有很多，比如："

```python
user_input = input("请输入年龄：")

if user_input.isdigit():
    age = int(user_input)
    print("你的年龄是：", age)
else:
    print("请输入数字！")
```

"但这种方法只能验证正整数。更好的方法是用`try-except`，我们以后会学。"

就在这时，苏晴举手："老师，我想分享一个我遇到的问题。"

众人都看向她。

"我昨天在做作业的时候，写了这样的代码："

```python
birth_year = input("请输入出生年份：")
current_year = 2024

age = current_year - birth_year
print("你的年龄是：", age)
```

"结果报错了：`TypeError: unsupported operand type(s) for -: 'int' and 'str'`。"

"我后来发现，是因为`birth_year`是字符串，不能直接和整数相减。"

"所以我改成了："

```python
birth_year = int(input("请输入出生年份："))
current_year = 2024

age = current_year - birth_year
print("你的年龄是：", age)
```

"这次就成功了。"

马老师点头："很好的分享。这就是类型混淆导致的bug。"

"记住，`input()`返回的永远是字符串。如果要用于计算，必须先转换。"

林北在心里默默记下。苏晴的经验分享很有用。

"最后，我给大家布置一个综合练习。"马老师说。

"写一个程序，实现以下功能："

"1. 接收用户输入的两个数字"
"2. 计算它们的和、差、积、商"
"3. 打印结果"

"要求：必须进行类型转换，必须处理除以0的情况。"

林北开始动手：

```python
num1 = float(input("请输入第一个数字："))
num2 = float(input("请输入第二个数字："))

# 计算
sum_result = num1 + num2
diff_result = num1 - num2
product_result = num1 * num2

# 除法需要特殊处理
if num2 != 0:
    quotient_result = num1 / num2
    print("商：", quotient_result)
else:
    print("商：无法计算（除数为0）")

# 打印结果
print("和：", sum_result)
print("差：", diff_result)
print("积：", product_result)
```

运行，输入10和5。

```
和： 15.0
差： 5.0
积： 50.0
商： 2.0
```

成功！林北满意地点点头。

下课后，三人又一起走出教室。

"今天学的陷阱，太有用了。"林北说，"特别是那个0.1加0.2的问题，太反直觉了。"

"对啊，"王胖子附和，"我本来以为编程就是加减乘除，没想到有这么多坑。"

"所以要多练。"苏晴说，"只有踩过坑，才知道怎么避坑。"

"你说得对。"林北感叹，"看来成为一个合格的程序员，不仅要学会语法，还要学会避坑。"

三人走到地铁站，分别的时候，林北突然问："苏晴，你之前学过编程吗？感觉你懂得挺多的。"

苏晴摇摇头："没有。只是我喜欢思考，遇到问题就会多试几次。"

"厉害。"林北由衷地说。

回到出租屋，林北打开电脑，开始总结今天学的内容。

他新建了一个文件`types_summary.md`，写下：

**数据类型总结**

1. **四种基本类型**
   - int：整数
   - float：浮点数
   - str：字符串
   - bool：布尔值

2. **类型转换**
   - `int()`：转整数
   - `float()`：转浮点数
   - `str()`：转字符串
   - `bool()`：转布尔值

3. **常见陷阱**
   - 字符串拼接 vs 数字相加
   - 浮点数精度问题
   - 整除 vs 真除法
   - 类型转换异常

4. **真值测试**
   - False：0、空字符串、None
   - True：其他所有值

5. **比较运算**
   - 返回布尔值
   - `>`、`<`、`>=`、`<=`、`==`、`!=`

林北看着这个总结，感觉自己对数据类型的理解已经比较全面了。

下节课要学`if`语句了，那将是程序能够"思考"的开始。

窗外，夜色渐深。

林北关上电脑，躺在床上。

他想起马老师说的那句话："编程不仅要学会语法，还要学会避坑。"

是啊，编程就像一场探险。路上充满了陷阱，但每避开一个坑，就离目标更近一步。

明天，他将学习如何让程序做出选择。

那将是编程世界里，一个全新的篇章……
