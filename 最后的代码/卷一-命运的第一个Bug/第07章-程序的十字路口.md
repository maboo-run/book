"今天我们继续学习条件判断。"

马老师在黑板上写下:"elif"。

"昨天我们学了if-else,可以处理两种情况。但现实中,很多问题不是非黑即白,而是有多种可能。"

"比如,成绩分为ABCDE五个等级。用if-else怎么写?"

林北想了想:"可以用嵌套的if-else吧?"

"对,但是会很丑。"马老师说,"让我演示一下。"

```python
score = int(input("请输入分数:"))

if score >= 90:
    print("A")
else:
    if score >= 80:
        print("B")
    else:
        if score >= 70:
            print("C")
        else:
            if score >= 60:
                print("D")
            else:
                print("E")
```

"看到了吗?嵌套了四层。缩进越来越深,像个金字塔。"

"如果条件更多,比如十个等级,那就要嵌套十层,代码会变成一个巨大的阶梯。"

林北看着这段代码,感觉密密麻麻的缩进让人头晕眼花。

"有更好的办法吗?"

"当然。"马老师擦掉黑板,重新写下:

```python
score = int(input("请输入分数:"))

if score >= 90:
    print("A")
elif score >= 80:
    print("B")
elif score >= 70:
    print("C")
elif score >= 60:
    print("D")
else:
    print("E")
```

"这就是elif,Else If的缩写,意思是'否则如果'。"

"看,同样的逻辑,代码扁平化了,没有深层嵌套,一目了然。"

林北眼睛一亮。确实,这样写清晰多了!

"elif的执行逻辑是这样的。"马老师在黑板上画了个流程图。

"从上往下依次检查条件:"
"1. 如果第一个条件(score >= 90)为真,执行对应代码,然后跳出整个if-elif-else结构"
"2. 如果第一个条件为假,检查第二个条件(score >= 80)"
"3. 如果第二个条件为真,执行对应代码,然后跳出"
"4. 以此类推,直到找到一个为真的条件,或者走到else"

"关键点:一旦找到一个为真的条件,后面的所有条件都不会再检查。"

"这就像走迷宫,一旦找到一个出口,你就不会再找其他出口了。"

"现在大家练习一下。"马老师说,"写一个程序,判断BMI的等级。"

"BMI小于18.5是偏瘦,18.5-24是正常,24-28是偏胖,28以上是肥胖。"

林北开始动手:

```python
height = float(input("请输入身高(米):"))
weight = float(input("请输入体重(公斤):"))

bmi = weight / (height ** 2)

print("你的BMI是:", bmi)

if bmi < 18.5:
    print("偏瘦")
elif bmi < 24:
    print("正常")
elif bmi < 28:
    print("偏胖")
else:
    print("肥胖")
```

运行,输入身高1.75,体重70:
```
你的BMI是: 22.857142857142858
正常
```

完美!比昨天那个嵌套的if-else清晰多了。

就在这时,李明举手:"老师,我有个问题。"

"如果我把条件的顺序换一下,比如先判断大于等于60,再判断大于等于90,会怎样?"

"好问题!"马老师赞许地说,"这就是elif最容易出错的地方——条件顺序。"

他写了个例子:

```python
score = 95

if score >= 60:
    print("及格")
elif score >= 90:
    print("优秀")
```

"大家猜猜,这个程序会输出什么?"

"优秀!"王胖子脱口而出。

"错。"马老师说,"是及格。"

"为什么?"王胖子惊呼,"95分明明是优秀啊!"

"因为95也大于等于60啊!"马老师解释道,"程序先检查第一个条件,发现95 >= 60为真,就执行打印'及格',然后跳出整个if-elif结构。"

"后面的'score >= 90'根本没机会执行。"

林北恍然大悟:"所以,条件要从严格到宽松排序?"

"对!"马老师点头,"应该先判断最严格的条件,再判断较宽松的条件。"

```python
score = 95

if score >= 90:
    print("优秀")
elif score >= 60:
    print("及格")
```

"这样,95分会先被判断为优秀。"

"现在我给大家出个思考题。"马老师说,"如果我想判断一个年份的季节,1-3月是春,4-6月是夏,7-9月是秋,10-12月是冬,应该怎么写?"

林北想了想:

```python
month = int(input("请输入月份:"))

if month >= 1 and month <= 3:
    print("春季")
elif month >= 4 and month <= 6:
    print("夏季")
elif month >= 7 and month <= 9:
    print("秋季")
elif month >= 10 and month <= 12:
    print("冬季")
else:
    print("月份错误")
```

"不错。"马老师说,"不过Python有个更简洁的写法。"

```python
month = int(input("请输入月份:"))

if 1 <= month <= 3:
    print("春季")
elif 4 <= month <= 6:
    print("夏季")
elif 7 <= month <= 9:
    print("秋季")
elif 10 <= month <= 12:
    print("冬季")
else:
    print("月份错误")
```

"看,Python允许连续比较。`1 <= month <= 3`相当于`month >= 1 and month <= 3`。"

"这是Python的语法糖,让代码更接近数学表达式。"

林北在笔记本上记下:Python支持连续比较,更简洁。

"现在,我给大家布置一个小组作业。"马老师说,"三人一组,写一个成绩查询系统。"

"要求:"
"1. 输入学生姓名和分数"
"2. 根据分数判断等级:90以上优秀,80-90良好,70-80中等,60-70及格,60以下不及格"
"3. 根据等级给出不同的评语"

林北、王胖子和苏晴自然地组成了一组。

"我们怎么分工?"林北问。

"我来写主逻辑。"苏晴说,"林北负责输入验证,王胖子负责输出美化。"

"好!"三人开始动手。

林北先写了输入部分:

```python
name = input("请输入学生姓名:")
score = int(input("请输入分数:"))

# 验证分数范围
if score < 0 or score > 100:
    print("分数错误!分数应该在0-100之间。")
else:
    # 后续逻辑
    pass
```

苏晴写了核心判断逻辑:

```python
if score >= 90:
    grade = "优秀"
    comment = "继续保持!"
elif score >= 80:
    grade = "良好"
    comment = "不错,再接再厉!"
elif score >= 70:
    grade = "中等"
    comment = "还可以,继续努力!"
elif score >= 60:
    grade = "及格"
    comment = "刚好及格,要加油了!"
else:
    grade = "不及格"
    comment = "需要更加努力!"
```

王胖子写了输出部分:

```python
print("=" * 30)
print(f"学生: {name}")
print(f"分数: {score}")
print(f"等级: {grade}")
print(f"评语: {comment}")
print("=" * 30)
```

三人把代码整合到一起:

```python
name = input("请输入学生姓名:")
score = int(input("请输入分数:"))

if score < 0 or score > 100:
    print("分数错误!分数应该在0-100之间。")
else:
    if score >= 90:
        grade = "优秀"
        comment = "继续保持!"
    elif score >= 80:
        grade = "良好"
        comment = "不错,再接再厉!"
    elif score >= 70:
        grade = "中等"
        comment = "还可以,继续努力!"
    elif score >= 60:
        grade = "及格"
        comment = "刚好及格,要加油了!"
    else:
        grade = "不及格"
        comment = "需要更加努力!"
    
    print("=" * 30)
    print(f"学生: {name}")
    print(f"分数: {score}")
    print(f"等级: {grade}")
    print(f"评语: {comment}")
    print("=" * 30)
```

运行,输入"林北",95:

```
==============================
学生: 林北
分数: 95
等级: 优秀
评语: 继续保持!
==============================
```

"成功了!"三人击掌庆祝。

马老师走过来检查:"不错。但是有个小问题。"

"如果用户输入的不是数字怎么办?比如输入'abc'?"

林北试了一下,程序报错:

```
ValueError: invalid literal for int() with base 10: 'abc'
```

"对啊,我们没有处理这种情况。"林北说。

"这需要用到异常处理。"马老师说,"我们以后会学。现在先假设用户输入正确。"

"现在我们来看看一些常见的坑。"马老师说。

"第一个坑:条件覆盖不全。"

```python
score = 85

if score > 90:
    print("优秀")
elif score > 80:
    print("良好")
# 缺少其他情况
```

"如果分数是70,这个程序什么都不输出。所以,一定要有else兜底。"

"第二个坑:条件重叠。"

```python
score = 85

if score >= 80:
    print("良好")
elif score >= 85:
    print("优秀")  # 这个永远不会执行
```

"85也大于等于80,所以会走第一个分支。第二个分支永远不会执行。"

"第三个坑:条件互斥但写成了独立的if。"

```python
score = 85

if score >= 90:
    print("优秀")

if score >= 80:
    print("良好")

if score >= 70:
    print("中等")
```

"这样写,85分会输出'良好'和'中等'两个等级!"

"正确的做法是用elif,保证只执行一个分支。"

就在这时,李明举手:"老师,我有个复杂的需求。"

"我想判断一个三角形的类型:等边三角形(三边相等),等腰三角形(两边相等),直角三角形(勾股定理),或者普通三角形。"

"而且,一个三角形可能既是等腰又是直角,该怎么处理?"

"好问题!"马老师说,"这涉及到条件判断的组合。"

"首先,要分清楚哪些条件是互斥的,哪些可以同时满足。"

"等边、等腰、普通三角形是互斥的,一个三角形只能是其中一种。"

"但直角三角形可以和等腰同时存在,比如等腰直角三角形。"

"所以,应该先判断边的关系,再判断角的关系。"

```python
a = float(input("请输入第一条边:"))
b = float(input("请输入第二条边:"))
c = float(input("请输入第三条边:"))

# 先判断能否构成三角形
if not ((a + b > c) and (b + c > a) and (a + c > b)):
    print("不能构成三角形")
else:
    # 判断边的关系
    if a == b == c:
        triangle_type = "等边三角形"
    elif a == b or b == c or a == c:
        triangle_type = "等腰三角形"
    else:
        triangle_type = "普通三角形"
    
    # 判断是否是直角三角形
    sides = sorted([a, b, c])
    is_right = abs(sides[0]**2 + sides[1]**2 - sides[2]**2) < 0.0001
    
    if is_right:
        print(f"这是{triangle_type},也是直角三角形")
    else:
        print(f"这是{triangle_type}")
```

"看,复杂的判断需要分层次,一步步缩小范围。"

下课前,马老师布置了作业:

"写一个简单的计算器,要求:"
"1. 输入两个数和运算符(+、-、*、/)"
"2. 根据运算符执行相应的计算"
"3. 处理除以0的情况"

林北开始思考。这需要用elif判断运算符……

```python
num1 = float(input("请输入第一个数:"))
operator = input("请输入运算符(+、-、*、/):")
num2 = float(input("请输入第二个数:"))

if operator == "+":
    result = num1 + num2
    print(f"{num1} + {num2} = {result}")
elif operator == "-":
    result = num1 - num2
    print(f"{num1} - {num2} = {result}")
elif operator == "*":
    result = num1 * num2
    print(f"{num1} * {num2} = {result}")
elif operator == "/":
    if num2 == 0:
        print("错误:除数不能为0!")
    else:
        result = num1 / num2
        print(f"{num1} / {num2} = {result}")
else:
    print("错误:不支持的运算符!")
```

运行,输入10,+,5:
```
请输入第一个数:10
请输入运算符(+、-、*、/):+
请输入第二个数:5
10.0 + 5.0 = 15.0
```

成功!林北很开心。

下课后,三人一起走出教室。

"今天的elif真有用。"林北说,"比嵌套的if-else清晰多了。"

"对啊,多分支判断变简单了。"王胖子说。

"不过要注意条件顺序。"苏晴提醒道,"从严格到宽松,这个很重要。"

"对对对!"林北附和,"今天李明演示的那个错误,印象太深刻了。95分被判成及格,太惨了。"

"还有就是要考虑全面。"苏晴说,"所有可能的情况都要覆盖到,最后用else兜底。"

"嗯。"林北点头,"写代码真的要很仔细。"

三人走到地铁站,分别的时候,王胖子突然说:"你们说,下节课会学什么?"

"应该是循环吧。"林北说,"老师提到过,要处理列表里的所有元素,需要循环。"

"期待!"苏晴微笑。

回到出租屋,林北开始复习今天学的内容。

他新建了一个文件`elif_practice.py`,开始尝试各种多分支判断:

```python
# 练习1:判断星期几
day = int(input("请输入数字(1-7):"))

if day == 1:
    print("星期一")
elif day == 2:
    print("星期二")
elif day == 3:
    print("星期三")
elif day == 4:
    print("星期四")
elif day == 5:
    print("星期五")
elif day == 6:
    print("星期六")
elif day == 7:
    print("星期日")
else:
    print("输入错误")

# 练习2:判断月份对应的季节
month = int(input("请输入月份:"))

if 1 <= month <= 3:
    print("春季")
elif 4 <= month <= 6:
    print("夏季")
elif 7 <= month <= 9:
    print("秋季")
elif 10 <= month <= 12:
    print("冬季")
else:
    print("月份错误")

# 练习3:判断字符类型
char = input("请输入一个字符:")

if char.isdigit():
    print("这是数字")
elif char.isalpha():
    print("这是字母")
elif char.isspace():
    print("这是空格")
else:
    print("这是特殊字符")
```

运行几遍,全部正确。

林北又想到了一个复杂的例子:个人所得税计算。

不同的收入区间,税率不同。这正好可以用elif来实现。

```python
income = float(input("请输入月收入:"))

# 假设起征点是5000
taxable = income - 5000

if taxable <= 0:
    tax = 0
    rate = 0
elif taxable <= 3000:
    tax = taxable * 0.03
    rate = 3
elif taxable <= 12000:
    tax = taxable * 0.1 - 210
    rate = 10
elif taxable <= 25000:
    tax = taxable * 0.2 - 1410
    rate = 20
else:
    tax = taxable * 0.25 - 2660
    rate = 25

print(f"应纳税额: {taxable}")
print(f"税率: {rate}%")
print(f"应纳税: {tax}")
print(f"税后收入: {income - tax}")
```

运行,输入10000:
```
应纳税额: 5000.0
税率: 10
应纳税: 290.0
税后收入: 9710.0
```

完美!林北感叹,elif真的很适合处理分段函数类型的问题。

林北关上电脑,躺在床上。

他想起马老师说的:"程序的十字路口。"

是啊,if是两条路,elif是多条路。

程序就像在迷宫里走,每遇到一个判断,就是一个岔路口。

选择哪条路,取决于条件。

而写代码的人,就是设计迷宫的人。

要确保每条路都有意义,每个岔路口都有明确的指示牌。

不能让"旅行者"(程序)迷路,也不能让他走进死胡同。

明天,他将学习循环。

那将是另一种控制流程的方式。

如果说条件判断是"选择",那循环就是"重复"。

一个让程序能做选择,一个让程序能不知疲倦地重复。

两者结合,程序就有了无穷的可能。

窗外,夜色渐深。

林北闭上眼睛,脑海里浮现出一个个岔路口。

每个路口都有清晰的标识:if、elif、else。

程序就像一个旅行者,在这些路口中穿梭。

而他,正在学习如何设计这些路口,如何引导程序走向正确的方向。
