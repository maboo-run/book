"复制……粘贴……再复制……再粘贴……"

林北双眼无神地盯着屏幕,手指机械地在Ctrl+C和Ctrl+V之间跳动。

为了练习while循环,他写了十几个小程序:素数判断器、闰年计算器、BMI计算器、个人所得税计算器……

这些程序里,有很多逻辑是重复的。比如"判断用户输入的是不是数字",这段代码在每个程序开头都要写一遍:

```python
user_input = input("请输入一个数字:")
if user_input.isdigit():
    num = int(user_input)
    # 后续逻辑
else:
    print("输入错误!")
```

这段代码,林北已经写了十几遍了。

更要命的是,刚才他发现这段判断代码有个小Bug——没处理小数和负数。

"完了。"林北哀嚎一声,"难道我要把这十几个程序全部打开,一个一个改过去?"

"这也太蠢了吧!这简直比搬砖还累!"

第二天,马老师走进教室,在黑板上写下一个大大的"DRY"。

"DRY,什么意思?"王胖子挠头。

"Don't Repeat Yourself。"马老师说,"不要重复你自己。"

"这是编程界的第一法则。任何一段代码,如果出现了两次以上,就说明你应该把它提炼出来,变成一个公用的工具。"

"这种工具,在编程里叫——函数。"

林北眼睛一亮。函数?这是什么神奇的东西?

"其实你们已经用过很多函数了。"马老师说。

"`print()`是函数,`input()`是函数,`len()`也是函数。"

"这些都是Python内置的函数,你们只需要调用它们,不需要知道它们内部是怎么实现的。"

"但现在,我要教你们如何创造自己的函数。"

马老师在屏幕上敲下:

```python
def say_hello():
    print("你好!")
    print("欢迎来到编程世界!")
```

"这就是一个函数。`def`是define的缩写,意思是'定义'。"

"`say_hello`是函数的名字,后面的括号是必须的,冒号也是必须的。"

"下面缩进的代码,就是函数体,也就是函数要执行的代码。"

"现在,我们定义好了这个函数,但它还不会执行。就像你画了个图纸,但东西还没造出来。"

"要让它执行,需要调用它:"

```python
say_hello()
```

运行:

```
你好!
欢迎来到编程世界!
```

"看,函数被调用了,里面的代码执行了。"

"现在大家练习一下。"马老师说,"写一个函数,打印你的自我介绍。"

林北开始动手:

```python
def introduce():
    print("我叫林北")
    print("我今年22岁")
    print("我喜欢打游戏")

introduce()
```

运行:

```
我叫林北
我今年22岁
我喜欢打游戏
```

成功!林北很开心。

"但是……"他突然想到一个问题,"如果我要介绍王胖子怎么办?再写一个`introduce_wang()`?"

"那如果要介绍全班50个人,难道要写50个函数?"

"好问题!"马老师说,"这就引出了函数的核心概念——参数。"

"参数就像函数的接口,让外部的数据能传进来。"

"举个例子,榨汁机。如果榨汁机里焊死了一个苹果,那它永远只能榨苹果汁。你想喝西瓜汁怎么办?"

"真正的榨汁机,应该留一个口子,让你往里面塞不同的水果。"

"函数也一样。"

马老师修改了代码:

```python
def introduce(name, age, hobby):
    print(f"我叫{name}")
    print(f"我今年{age}岁")
    print(f"我喜欢{hobby}")

introduce("林北", 22, "打游戏")
introduce("王胖子", 22, "吃火锅")
introduce("苏晴", 21, "看书")
```

运行:

```
我叫林北
我今年22岁
我喜欢打游戏
我叫王胖子
我今年22岁
我喜欢吃火锅
我叫苏晴
我今年21岁
我喜欢看书
```

"看,同一个函数,传入不同的参数,就能处理不同的数据。"

林北恍然大悟。原来参数就是函数的"接口"!

"参数有两种。"马老师继续说,"定义函数时的参数叫'形参',调用函数时传入的值叫'实参'。"

"比如:"

```python
def greet(name):  # name是形参
    print(f"你好,{name}")

greet("林北")  # "林北"是实参
```

"形参就像占位符,实参是真正的值。"

"调用函数时,实参会赋值给形参,然后函数体就能使用这个值了。"

李明举手:"老师,参数可以有多个吗?"

"当然。"马老师说,"可以有任意多个,用逗号隔开。"

```python
def calculate_bmi(height, weight):
    bmi = weight / (height ** 2)
    print(f"身高{height}米,体重{weight}公斤,BMI是{bmi}")

calculate_bmi(1.75, 70)
```

运行:

```
身高1.75米,体重70公斤,BMI是22.857142857142858
```

就在这时,王胖子举手:"老师,我有个问题。"

"如果我调用函数时,参数的顺序搞反了会怎样?"

"好问题。"马老师说,"试试看。"

王胖子写下:

```python
def calculate_bmi(height, weight):
    bmi = weight / (height ** 2)
    print(f"身高{height}米,体重{weight}公斤,BMI是{bmi}")

calculate_bmi(70, 1.75)  # 把体重和身高搞反了
```

运行:

```
身高70米,体重1.75公斤,BMI是0.00035714285714285714
```

全班哄堂大笑。

"看,结果完全错了。"马老师说,"70米高,1.75公斤重,这是什么怪物?"

"所以,调用函数时,参数的顺序必须和定义时的顺序一致。"

"除非……"马老师话锋一转,"你使用关键字参数。"

```python
calculate_bmi(weight=70, height=1.75)
```

"这样,不管顺序如何,Python都能正确匹配参数。"

"现在我给大家布置一个小组作业。"马老师说,"三人一组,写一个温度转换器。"

"要求:"
"1. 写一个函数,把摄氏度转华氏度"
"2. 写一个函数,把华氏度转摄氏度"
"3. 写一个主程序,让用户选择转换方向,输入温度,调用相应函数"

林北、王胖子和苏晴又组成了一组。

"我来写转换函数。"苏晴说。

```python
def celsius_to_fahrenheit(c):
    f = c * 9 / 5 + 32
    return f

def fahrenheit_to_celsius(f):
    c = (f - 32) * 5 / 9
    return c
```

"等等,这个`return`是什么?"林北问。

"是返回值。"苏晴解释道,"函数计算出结果后,需要把结果传回去。"

"就像你去餐厅点菜,厨师做好了要端给你,不能做好了自己吃了。"

"哦!"林北恍然大悟。

王胖子写了主程序:

```python
print("温度转换器")
print("1. 摄氏度转华氏度")
print("2. 华氏度转摄氏度")

choice = input("请选择(1或2):")

if choice == "1":
    c = float(input("请输入摄氏度:"))
    f = celsius_to_fahrenheit(c)
    print(f"{c}°C = {f}°F")
elif choice == "2":
    f = float(input("请输入华氏度:"))
    c = fahrenheit_to_celsius(f)
    print(f"{f}°F = {c}°C")
else:
    print("无效的选择")
```

三人把代码整合到一起,运行:

```
温度转换器
1. 摄氏度转华氏度
2. 华氏度转摄氏度
请选择(1或2):1
请输入摄氏度:37
37.0°C = 98.6°F
```

"成功了!"三人击掌庆祝。

马老师走过来检查:"不错。但我有个建议。"

"你们的函数名很好,但可以加点注释,说明函数的作用。"

```python
def celsius_to_fahrenheit(c):
    """
    将摄氏度转换为华氏度
    参数: c - 摄氏度
    返回: 华氏度
    """
    f = c * 9 / 5 + 32
    return f
```

"这种注释叫文档字符串,用三个引号括起来。"

"它能帮助别人(包括未来的你)理解函数的作用。"

下课前,马老师说:"函数的好处,不仅是避免重复,更重要的是让代码结构更清晰。"

"一个复杂的程序,如果全部写在一起,会变成一团乱麻。"

"但如果拆分成多个函数,每个函数负责一个小任务,整个程序就清晰多了。"

"这叫模块化思想。"

"以后你们写项目,一定要养成把功能拆分成函数的习惯。"

下课后,三人一起走出教室。

"今天学的函数,感觉很重要啊。"林北说。

"对啊,以前写代码都是一大坨,现在可以拆分成小块了。"王胖子说。

"而且函数可以复用。"苏晴补充道,"写一次,用无数次。"

"对对对!"林北兴奋地说,"以前我要改一个逻辑,得在十几个地方改。现在只要改一个函数,所有调用它的地方都自动更新了!"

"不过我有个问题。"王胖子说,"那个`return`到底是干什么的?"

"是返回值。"苏晴说,"函数计算出结果后,用return把结果传回去。"

"如果没有return呢?"

"那函数就只是执行代码,不返回任何东西。"

"哦,懂了。"

回到出租屋,林北开始复习今天学的内容。

他新建了一个文件`functions_practice.py`,开始尝试各种函数:

```python
# 练习1:问候函数
def greet(name):
    print(f"你好,{name}!")

greet("林北")
greet("王胖子")
greet("苏晴")

# 练习2:计算圆面积
def circle_area(radius):
    area = 3.14 * radius ** 2
    return area

print("半径为5的圆面积:", circle_area(5))

# 练习3:判断奇偶
def is_even(num):
    if num % 2 == 0:
        return True
    else:
        return False

print("10是偶数吗?", is_even(10))
print("7是偶数吗?", is_even(7))

# 练习4:找最大值
def find_max(a, b, c):
    if a >= b and a >= c:
        return a
    elif b >= a and b >= c:
        return b
    else:
        return c

print("10, 25, 18的最大值是:", find_max(10, 25, 18))
```

运行几遍,全部正确。

林北又想到了一个有趣的问题:能不能写一个函数来生成密码?

```python
import random

def generate_password(length):
    """
    生成指定长度的随机密码
    参数: length - 密码长度
    返回: 随机密码字符串
    """
    chars = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789"
    password = ""
    count = 0
    
    while count < length:
        password = password + random.choice(chars)
        count = count + 1
    
    return password

print("8位密码:", generate_password(8))
print("12位密码:", generate_password(12))
print("16位密码:", generate_password(16))
```

运行:

```
8位密码: aB3xK9mP
12位密码: 7hTy2QwE5nLp
16位密码: mN4uJ8rX6sV2kH9c
```

成功!林北感叹,函数真是太强大了。

只要定义好函数,就能像搭积木一样组合各种功能。

林北又尝试把之前写的BMI计算器改成函数版本:

```python
def calculate_bmi(height, weight):
    """计算BMI指数"""
    bmi = weight / (height ** 2)
    return bmi

def get_bmi_category(bmi):
    """根据BMI判断体重类别"""
    if bmi < 18.5:
        return "偏瘦"
    elif bmi < 24:
        return "正常"
    elif bmi < 28:
        return "偏胖"
    else:
        return "肥胖"

# 主程序
height = float(input("请输入身高(米):"))
weight = float(input("请输入体重(公斤):"))

bmi = calculate_bmi(height, weight)
category = get_bmi_category(bmi)

print(f"你的BMI是: {bmi:.2f}")
print(f"体重状况: {category}")
```

运行,输入1.75和70:

```
你的BMI是: 22.86
体重状况: 正常
```

林北看着这个代码,感觉比之前写的清晰多了。

每个函数只负责一个任务:
- `calculate_bmi`负责计算BMI
- `get_bmi_category`负责判断类别
- 主程序负责协调

这就是模块化思想!

林北关上电脑,躺在床上。

他想起马老师说的:"函数让代码结构更清晰。"

是啊,如果把程序比作一栋大楼,那函数就是大楼里的一个个房间。

每个房间负责不同的功能:卧室、厨房、卫生间……

你不会把所有功能都塞在一个大通间里,那会乱成一团。

同样,你也不应该把所有代码都写在一起,那会变成一团乱麻。

函数就是帮你把代码分割成一个个独立的模块。

每个模块做好自己的事,然后组合起来,完成复杂的任务。

这就是编程的艺术。

林北闭上眼睛。

脑海里浮现出一个个函数,像乐高积木一样。

你可以把它们组合成各种形状:
- 计算器
- 游戏
- 网站
- APP

只要有足够的函数,你就能搭建出任何你想要的东西。

明天,他将学习函数的参数。

据说还有默认参数、可变参数、关键字参数……

那将是函数世界更深入的探索。

窗外,夜色渐深。

林北想起那个温度转换器,想起那个密码生成器,想起那个BMI计算器。

函数,让程序有了灵魂。

它不再是一堆散乱的代码,而是一个个精心设计的工具。

而他,正在学习如何打造这些工具……
