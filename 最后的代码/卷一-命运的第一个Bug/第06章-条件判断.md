"今天我们要学习程序最重要的能力之一——选择。"

马老师在黑板上写下两个大字:"If"。

"生活充满了选择。"马老师的声音在教室里回荡,"早上闹钟响了,如果困,就再睡五分钟;如果不困,就起床。"

"中午去食堂,如果有红烧肉,就排队;如果没有,就吃泡面。"

"遇到喜欢的女孩,如果她也喜欢你,就表白;如果她不喜欢你……"马老师顿了顿,"那就暗恋。"

台下一阵哄笑。

林北也笑了,但心里却在想:程序也能做选择吗？

"程序也是一样。"马老师接着说,"没有if的程序,就是个傻子,一条道走到黑。不管前面是悬崖还是金矿,它都只会闷头往前冲。"

"今天,我们要教会电脑一件事情——看路。"

"先从最简单的开始:单分支if语句。"马老师说。

```python
age = 20

if age >= 18:
    print("你已经成年了")

print("程序结束")
```

"这个程序的逻辑是:如果年龄大于等于18,就打印'你已经成年了'。不管条件是否满足,最后都会打印'程序结束'。"

马老师运行了一遍:

```
你已经成年了
程序结束
```

"现在我把年龄改成16:"

```python
age = 16

if age >= 18:
    print("你已经成年了")

print("程序结束")
```

运行:

```
程序结束
```

"看,如果条件不满足,if里面的代码就不会执行,程序直接跳过。"

林北恍然大悟。原来if就是一个可选的分支,走不走取决于条件。

"注意看这个缩进。"马老师指着`print`前面的空白,"这是Python里最重要的语法规则之一。"

"在Python里,缩进不仅是为了美观,更是语法的一部分。它代表了代码块的从属关系。"

"缩进的代码属于if语句,只有当条件为真时才会执行。没有缩进的代码不属于if,会正常执行。"

马老师写了个例子:

```python
age = 16

if age >= 18:
    print("你已经成年了")
    print("可以独自出行")

print("程序结束")
```

"这里,前两个print都缩进了,所以都属于if。如果age小于18,这两行都不会执行。"

"但最后一个print没有缩进,所以不管条件是否满足,它都会执行。"

"如果缩进错了会怎样?"王胖子问。

"好问题。"马老师说,"缩进错误是新手最常犯的错误之一。"

他写了个错误的例子:

```python
age = 20

if age >= 18:
print("你已经成年了")  # 忘记缩进
```

运行,报错:

```
IndentationError: expected an indented block
```

"这个错误的意思是:我期待一个缩进的代码块,但你没给。"

"Python对缩进非常严格。必须有缩进,而且必须一致。"

马老师又写了个例子:

```python
age = 20

if age >= 18:
    print("你已经成年了")
        print("可以独自出行")  # 缩进不一致
```

运行,报错:

```
IndentationError: unexpected indent
```

"这个错误的意思是:你的缩进不一致,我搞不清楚哪些代码属于if。"

"所以,记住:Python用4个空格作为标准缩进。虽然Tab键也可以,但不要混用空格和Tab,否则会出现奇怪的错误。"

林北在笔记本上记下:缩进用4个空格,不要混用空格和Tab。

就在这时,李明举手:"老师,如果我想在条件不满足的时候做另一件事,应该怎么写?"

"好问题!这就是双分支if-else语句。"马老师说。

```python
age = int(input("请输入你的年龄:"))

if age >= 18:
    print("你已经成年了")
    print("欢迎光临网吧")
else:
    print("你还未成年")
    print("请回家写作业")

print("程序结束")
```

"这个程序有两条路:如果年龄大于等于18,走if分支;否则,走else分支。"

"注意,if和else是互斥的,只会执行其中一个,不可能两个都执行。"

马老师运行了几次,输入不同的年龄:

输入20:
```
你已经成年了
欢迎光临网吧
程序结束
```

输入16:
```
你还未成年
请回家写作业
程序结束
```

"看,根据条件的不同,程序会做出不同的选择。这就是if-else的魔力。"

"现在大家练习一下。"马老师说,"写一个程序,判断一个数是奇数还是偶数。"

"提示:用取余运算符`%`。偶数除以2余数是0,奇数除以2余数是1。"

林北开始动手:

```python
num = int(input("请输入一个数:"))

if num % 2 == 0:
    print("这是偶数")
else:
    print("这是奇数")
```

运行,输入10:
```
这是偶数
```

输入7:
```
这是奇数
```

成功了!林北很开心。

就在这时,王胖子那边传来了声音:"老师!我的程序不对!"

众人看过去,只见他的屏幕上显示:

```python
num = int(input("请输入一个数:"))

if num % 2 == 0:
    print("这是偶数")
else:
print("这是奇数")  # 忘记缩进
```

报错:
```
IndentationError: expected an indented block
```

"你的else后面忘记缩进了。"马老师提醒道。

"哦!"王胖子赶紧加上缩进,这次成功了。

"看,缩进错误是最常见的问题。"马老师说,"大家一定要注意。"

"现在我们来看看布尔表达式。"马老师说,"if后面的条件,可以是任何返回布尔值的表达式。"

"比如:"

```python
# 比较运算符
if age >= 18:
    print("成年了")

# 逻辑运算符
if age >= 18 and has_id:
    print("可以进网吧")

# 变量本身
if is_student:
    print("你是学生")

# 函数返回值
if len(name) > 0:
    print("名字不为空")
```

"只要表达式的结果是True或False,都可以放在if后面。"

李明举手:"老师,如果我想判断多个条件,应该怎么写?"

"用逻辑运算符。"马老师说,"and、or、not。"

```python
age = 20
has_id = True

# and:两个条件都要满足
if age >= 18 and has_id:
    print("可以进网吧")

# or:只要有一个条件满足
is_student = True
is_senior = False

if is_student or is_senior:
    print("有优惠")

# not:取反
if not is_raining:
    print("不用带伞")
```

"记住,and的优先级高于or。如果表达式复杂,最好用括号明确优先级。"

苏晴举手:"老师,我有个问题。如果我想判断一个分数的等级,比如90分以上是优秀,80-90是良好,60-80是及格,60以下是不及格,应该怎么写?"

"好问题!"马老师说,"这需要多个条件判断。"

"但是,用if-else只能处理两种情况。如果有三种、四种、甚至更多种情况,该怎么办?"

林北也想到了这个问题。对啊,if-else只能二选一,如果有多个选项呢?

"这就需要用到elif。"马老师说,"不过这个我们下节课再讲。"

"今天,大家先把if-else练熟。"

"现在我给大家布置一个综合练习。"马老师说。

"写一个登录系统,要求:"
"1. 接收用户输入的用户名和密码"
"2. 如果用户名是'admin'并且密码是'123456',显示'登录成功'"
"3. 否则,显示'用户名或密码错误'"

林北开始动手:

```python
username = input("请输入用户名:")
password = input("请输入密码:")

if username == "admin" and password == "123456":
    print("登录成功")
    print("欢迎回来,管理员")
else:
    print("用户名或密码错误")
    print("请重新输入")
```

运行,输入正确的用户名和密码:
```
请输入用户名:admin
请输入密码:123456
登录成功
欢迎回来,管理员
```

输入错误的:
```
请输入用户名:user
请输入密码:111111
用户名或密码错误
请重新输入
```

完美!林北满意地点点头。

就在这时,李明举手:"老师,我写了个更复杂的版本。"

他展示了自己的代码:

```python
username = input("请输入用户名:")
password = input("请输入密码:")

if username == "admin":
    if password == "123456":
        print("登录成功")
    else:
        print("密码错误")
else:
    print("用户名不存在")
```

"这是嵌套的if语句。"李明解释道,"先判断用户名,再判断密码。"

"很好!"马老师赞许地说,"这就是if的嵌套。在if里面可以再写if。"

"不过,嵌套太多会让代码难读。一般建议不要超过三层。"

"而且,李明这个逻辑有个小问题:如果用户名对了但密码错了,会显示'密码错误';但如果用户名错了,就直接说'用户名不存在',连密码都不检查。"

"这样做其实是有安全隐患的,因为黑客可以通过这个信息判断用户名是否存在。"

"所以,更好的做法是像林北那样,只显示'用户名或密码错误',不透露具体是哪个错了。"

李明恍然大悟:"原来如此!我学到了。"

林北在心里默默记下:写代码不仅要考虑功能,还要考虑安全性。

"现在我们来看看一些常见的坑。"马老师说。

"第一个坑:忘记冒号。"

```python
if age >= 18  # 忘记冒号
    print("成年了")
```

运行,报错:
```
SyntaxError: invalid syntax
```

"if和else后面必须有冒号。这是Python的语法规则。"

"第二个坑:用单等号而不是双等号。"

```python
if age = 18:  # 错误!应该用==
    print("18岁")
```

运行,报错:
```
SyntaxError: invalid syntax
```

"记住,赋值用单等号,比较用双等号。"

"第三个坑:缩进不一致。"

```python
if age >= 18:
    print("成年了")
  print("可以出行")  # 缩进只有2个空格
```

运行,报错:
```
IndentationError: unindent does not match any outer indentation level
```

"缩进必须一致,要么都是4个空格,要么都是Tab,不能混用。"

"第四个坑:条件写反了。"

```python
if age < 18:
    print("成年了")  # 逻辑错误!
else:
    print("未成年")
```

"这个不会报错,但逻辑是错的。小于18岁却说成年了,这是Bug。"

"所以,写完代码一定要测试,确保逻辑正确。"

下课前,王胖子问:"老师,如果我要判断一个年份是否是闰年,应该怎么写?"

"闰年的规则是:能被4整除但不能被100整除,或者能被400整除。"

"好问题。"马老师说,"这需要组合多个条件。"

```python
year = int(input("请输入年份:"))

if (year % 4 == 0 and year % 100 != 0) or (year % 400 == 0):
    print(year, "是闰年")
else:
    print(year, "不是闰年")
```

"看,用括号把条件分组,逻辑就很清晰了。"

"先判断'能被4整除但不能被100整除',或者'能被400整除'。"

"满足任意一个条件,就是闰年。"

林北在笔记本上记下这个例子。复杂的条件判断,需要仔细设计逻辑。

下课后,林北、王胖子和苏晴一起走出教室。

"今天学的if-else,感觉很有用啊。"林北说。

"对啊,程序终于能做选择了!"王胖子兴奋地说,"以前就像个傻子,现在感觉有点智能了。"

"不过我有个问题。"苏晴说,"如果有多个条件,比如成绩分ABCDE五个等级,用if-else写会不会很麻烦?"

"对啊!"林北也想到了这个问题,"需要嵌套好多层if-else吧?"

"应该有更好的方法。"苏晴若有所思,"比如那个elif什么的。"

"对,老师说了,下节课会讲。"林北说。

"期待!"王胖子说。

回到出租屋,林北开始复习今天学的内容。

他新建了一个文件`if_else_practice.py`,开始尝试各种条件判断:

```python
# 练习1:判断奇偶
num = int(input("请输入一个数:"))
if num % 2 == 0:
    print("偶数")
else:
    print("奇数")

# 练习2:判断正负
num = int(input("请输入一个数:"))
if num > 0:
    print("正数")
else:
    if num < 0:
        print("负数")
    else:
        print("零")

# 练习3:判断三角形
a = float(input("请输入第一条边:"))
b = float(input("请输入第二条边:"))
c = float(input("请输入第三条边:"))

if (a + b > c) and (b + c > a) and (a + c > b):
    print("可以构成三角形")
else:
    print("不能构成三角形")
```

运行几遍,全部正确。

林北又想到了一个问题:如果要写一个BMI计算器,不仅要计算BMI,还要根据BMI判断体重状态,该怎么写?

BMI小于18.5是偏瘦,18.5-24是正常,24-28是偏胖,28以上是肥胖。

林北试着写:

```python
height = float(input("请输入身高(米):"))
weight = float(input("请输入体重(公斤):"))

bmi = weight / (height ** 2)

print("你的BMI是:", bmi)

if bmi < 18.5:
    print("偏瘦")
else:
    if bmi < 24:
        print("正常")
    else:
        if bmi < 28:
            print("偏胖")
        else:
            print("肥胖")
```

运行,输入身高1.75,体重70:
```
你的BMI是: 22.857142857142858
正常
```

成功了!但林北看着这个嵌套的if-else,感觉有点别扭。

这样写太丑了,而且如果条件更多,嵌套会更深。

他想起苏晴说的,应该有更好的方法。

"下节课的elif,应该就是解决这个问题的吧。"林北想。

林北关上电脑,躺在床上。

他想起马老师说的:"没有if的程序,就是个傻子,一条道走到黑。"

现在,程序终于能"看路"了,能根据不同的情况做出不同的选择。

虽然只是简单的if-else,但这已经让程序有了"智能"的雏形。

如果说变量是程序的记忆,运算符是程序的计算能力,那么if就是程序的判断能力。

有了这三样,程序就能做很多事情了。

但林北也意识到,if-else只能处理两种情况。如果有多种情况,嵌套会很复杂。

他期待着明天的课程。

elif,听起来就像是为多分支判断而生的。

窗外,夜色渐深。

林北闭上眼睛,脑海里浮现出一个个分叉的路口。

程序就像一辆车,在代码的世界里行驶。

每遇到一个if,就是一个路口。

选择左边还是右边,取决于条件是True还是False。

而明天,他将学会如何在更复杂的路口中做出选择。

那将是编程世界里,又一个重要的里程碑……
